import { SortByVotePipe } from './sort-by-vote.pipe';

describe('SortByVotePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByVotePipe();
    expect(pipe).toBeTruthy();
  });
});
